# Fused Posit Simulation Assets

This directory contains simulation artifacts, log files, and input datasets for verifying the Fused Posit FFT architecture. It includes the real-world 1024-point radar input vectors (`real_inputs_1024.txt`) used to test numeric accuracy. The compiled Icarus Verilog simulation executables and corresponding simulator logs containing the computed outputs are also stored here for analytical comparisons.
