# Non-Fused Posit Simulation Assets

This directory contains the simulation inputs, compilation artifacts, and log files for testing the baseline Non-Fused Posit FFT. It includes the 1024-point radar sample input dataset, the compiled test simulation files, and the output logs containing the numerical results generated during simulation. These outputs are compared against reference datasets to calculate key performance indicators such as RMSE and SNR.
